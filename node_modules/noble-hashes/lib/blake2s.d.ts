import * as blake2 from './_blake2';
export declare const blake2s: {
    (msg: import("./utils").Input, opts: blake2.BlakeOpts): Uint8Array;
    outputLen: number;
    blockLen: number;
    create(opts: blake2.BlakeOpts): import("./utils").Hash;
    init: (opts: blake2.BlakeOpts) => import("./utils").Hash;
};
